<!-- res/layout/activity_product.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <!-- Content Container -->

    <!-- Bottom Navigation Bar -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation"
        android:layout_marginBottom="2dp"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Part Image -->
            <ImageView
                android:id="@+id/part_image"
                android:layout_width="match_parent"
                android:layout_height="395dp"
                android:background="@drawable/rounded_corner"
                android:clipToOutline="true"
                android:scaleType="centerCrop"
                android:src="@drawable/white_pc_build_featured_image_1_1" />

            <!-- Part Name -->
            <TextView
                android:id="@+id/part_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingTop="8dp"
                android:paddingBottom="4dp"
                android:text="PC Component XYZ"
                android:textSize="24sp"
                android:textStyle="bold" />

            <!-- Part Price -->
            <TextView
                android:id="@+id/part_price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingBottom="12dp"
                android:text="Tk 29999"
                android:textColor="#431212"
                android:textSize="20sp" />

            <!-- Part Description -->
            <TextView
                android:id="@+id/part_description"
                android:layout_width="wrap_content"
                android:layout_height="105dp"
                android:paddingBottom="16dp"
                android:text="This PC component is designed for optimal performance..."
                android:textSize="16sp" />

            <!-- Add to Cart Button -->
            <Button
                android:id="@+id/btn_add_to_cart"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:backgroundTint="#000000"
                android:textSize="16dp"
                android:text="Add to Cart"
                android:textColor="@android:color/white" />
        </LinearLayout>
    </ScrollView>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_alignParentBottom="true"
        android:background="#2196F3" />
</RelativeLayout>
